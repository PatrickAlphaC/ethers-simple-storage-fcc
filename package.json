{
  "dependencies": {
    "dotenv": "^14.2.0",
    "ethers": "^5.5.3",
    "hardhat": "^2.8.3",
    "prettier": "^2.5.1",
    "solc": "0.8.7-fixed"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_modules/ --base-path . -o . SimpleStorage.sol"
  }
}
